spec:
  template:
    spec:
      containers:
      - name: tcpdumper
        image: docker.io/dockersec/tcpdump
        command:
        - "/bin/bash"
        - "-c"
        - "tcpdump -w /tmp/tcpdump.pcap"
